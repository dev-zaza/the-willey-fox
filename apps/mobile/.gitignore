# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Expo local tooling and caches
.expo/

# Dependencies (workspace may hoist; this app can still have a local install)
node_modules/

# Environment — keep .env.example committed
.env
.env.local
.env.*.local

# Metro
.metro-health-check*

# Expo web / export output
dist/
web-build/

# Logs and debug
*.log
npm-debug.*
yarn-debug.*
yarn-error.*
pnpm-debug.*

# Testing
coverage/

# TypeScript
*.tsbuildinfo

# Signing / store secrets (never commit)
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.pem

# EAS — local credentials (use EAS servers or env in CI)
credentials.json

# Firebase / FCM — never commit
google-services.json
GoogleService-Info.plist

# Android prebuild / bare workflow (when present)
android/app/build/
android/build/
android/.gradle/
android/local.properties
*.apk
*.aab

# Fastlane (if used)
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots
fastlane/test_output

.env.local

.env.local
